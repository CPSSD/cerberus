pipeline:
    build:
        image: goldenbadger/cerberus-ci
        commands:
            - cargo build --all
            - cargo test --all

    clippy:
        image: goldenbadger/cerberus-ci
        commands:
            - rustup install nightly
            - rustup default nightly
            - cargo install clippy
            - cargo clippy --all
